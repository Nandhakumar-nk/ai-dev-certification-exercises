Identify code smells in this codebase and suggest improvements:
1. Look for duplicated logic, overly long functions, and deep nesting
2. Flag poor separation of concerns (e.g. business logic mixed into route handlers)
3. Flag inconsistent patterns across similar files (e.g. differing error handling, differing validation)
4. Note any unnecessary complexity that could be simplified

For each smell: file, line, description, suggested refactor.
