Analyze test coverage for this project:
1. Run the test suite with coverage (npm test -- --coverage; if no coverage tool is installed, analyze coverage manually by comparing source files to their test files)
2. Identify files with less than 80% coverage
3. Suggest specific tests to add for uncovered code paths
4. Prioritize by risk: auth, payments, and data handling first
